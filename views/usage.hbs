 <script type="text/javascript" src="vis/dist/vis.js"></script>
<link href="../css/vis-timeline-graph2d.min.css" rel="stylesheet" type="text/css" /> 
 

<!-- Page Heading/Breadcrumbs -->
<div class="row">
      <div class="col-lg-12">
          <h1 class="page-header">Robot Usage</h1>
          <ol class="breadcrumb">
              <li><a href="/">Home</a>
              </li>
              <li class="active">Usage</li>
          </ol>
      </div>
</div>

<div class="row">
      <div class="col-md-10">
          <h3>Daily Usage</h3>
          <div id="visualization"></div>
      </div>
</div>

{{!--<div class="row">
      <div class="col-md-10">
          <h3>Month Usage</h3>
          
      </div>
</div>--}}

<script type="text/javascript">
        var counter = [];
        var year = [];
        var month = [];
        var day = [];

        //push the got data into array
        counter.push( {{count}} );
        year.push( {{year}} );
        month.push( {{month}} );
        day.push( {{day}} );

        //print daily count
        {{!--for(var i in counter)
        {
            console.log(counter[i]);
        }--}}

        //print date
        for(var i = 0; i < year.length; i++)
        {
            console.log(year[i]+"/"+month[i]+"/"+day[i]);
        }

        //Construct Graph
        var container = document.getElementById('visualization');
        var items = [];
        var dateArray = [];
        var d = new Date();

        for(var i = 0; i < year.length; i++){
           items.push( {x: d.setFullYear(year[i],month[i]-1,day[i]), y: counter[i]} );
            console.log(d);
        }

        var dataset = new vis.DataSet(items);
        var options = {
            style:'bar',
            barChart: {width:40, align:'center'}, // align: left, center, right
            drawPoints: false,
            dataAxis: {
                icons:true
            },
            orientation:'top',
            start: year[0]+'-'+month[0]+'-'+day[0],
            end: year[year.length-1]+'-'+month[month.length-1]+'-'+day[day.length-1]
            {{!--start: '2017-02-25',
            end: '2017-03-17'--}}
        };
        var graph2d = new vis.Graph2d(container, items, options);
</script>

